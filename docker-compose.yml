services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./server/database.sqlite:/app/server/database.sqlite
    environment:
      - PORT=3000
      - NODE_ENV=production
      - JWT_SECRET=dev_secret_key_123
